# R for travis: see documentation at https://docs.travis-ci.com/user/languages/r

matrix:
  include:
    - os: linux
      dist: bionic
      r: devel

    - os: linux
      dist: bionic
      r: release

  allow_failures:
    - os: linux
      dist: bionic
      r: devel


language: R
sudo: false
cache: packages

notifications:
  email:
    on_success: never
    on_failure: never